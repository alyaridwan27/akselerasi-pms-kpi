// src/utils/devSeedKPIs.ts
// 🚫 KPI SEEDING DISABLED — kept only to avoid breaking imports.

export async function seedKPIsForUser(userId: string) {
  console.log(`🔹 seedKPIsForUser skipped for user ${userId} (seeding disabled).`);
  return;
}
